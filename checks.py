import sys

#does this support decimals py3.6 best version :weary::tm:
if sys.version_info[0] < 3:
    raise Exception('Must be running Python version 3 or up')

#ok do we need anything else
#i think not
#lets run haah yes
